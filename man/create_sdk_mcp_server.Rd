% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdk_mcp.R
\name{create_sdk_mcp_server}
\alias{create_sdk_mcp_server}
\title{Create an in-process SDK MCP server}
\usage{
create_sdk_mcp_server(name, version = "1.0.0", tools = NULL)
}
\arguments{
\item{name}{Character(1). Server identifier referenced in \code{mcp_servers}.}

\item{version}{Character(1). Informational server version.}

\item{tools}{List of \code{SdkMcpTool} objects (from \code{\link[=sdk_mcp_tool]{sdk_mcp_tool()}}).}
}
\value{
An object of class \code{McpSdkServerConfig}: a list with \code{type = "sdk"},
\code{name}, \code{version}, and an \code{instance} holding the tool registry. The
transport strips \code{instance} before forwarding the config to the CLI; the
registry stays in-process to dispatch tool calls.
}
\description{
Bundles \code{\link[=sdk_mcp_tool]{sdk_mcp_tool()}} definitions into a server configuration that runs
inside the R SDK process. Pass it inside \verb{ClaudeAgentOptions(mcp_servers = list(<name> = create_sdk_mcp_server(...)))}. The R analogue of the Python
SDK's \code{create_sdk_mcp_server()}.
}
\examples{
add <- sdk_mcp_tool(
  "add", "Add two numbers", list(a = "number", b = "number"),
  function(args) list(content = list(list(
    type = "text", text = paste0("Sum: ", args$a + args$b))))
)
server <- create_sdk_mcp_server("calc", version = "1.0.0", tools = list(add))
\dontrun{
opts <- ClaudeAgentOptions(
  mcp_servers   = list(calc = server),
  allowed_tools = "mcp__calc__add"
)
}
}
\seealso{
\code{\link[=sdk_mcp_tool]{sdk_mcp_tool()}}
}
