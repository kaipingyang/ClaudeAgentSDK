% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_mutations.R
\name{fork_session}
\alias{fork_session}
\title{Fork a session}
\usage{
fork_session(
  session_id,
  directory = NULL,
  up_to_message_id = NULL,
  title = NULL
)
}
\arguments{
\item{session_id}{Character. UUID of the source session.}

\item{directory}{Character or NULL. Project directory.}

\item{up_to_message_id}{Character or NULL. Slice transcript up to this
message UUID (inclusive). \code{NULL} copies the full transcript.}

\item{title}{Character or NULL. Custom title for the fork. If \code{NULL},
derives title from the original + " (fork)".}
}
\value{
Named list with \code{session_id} — the UUID of the new forked session.
}
\description{
Copies transcript messages from the source session into a new JSONL file
with fresh UUIDs, preserving the \code{parentUuid} chain. Supports
\code{up_to_message_id} for branching from a specific point.
}
\examples{
\donttest{
sessions <- list_sessions(limit = 1L)
if (length(sessions) > 0) {
  forked <- fork_session(sessions[[1]]$session_id, title = "Branch A")
  forked$session_id  # UUID of the new session
}
}
}
