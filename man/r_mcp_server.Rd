% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{r_mcp_server}
\alias{r_mcp_server}
\title{Create an R-based MCP server entry for \code{mcp_servers}}
\usage{
r_mcp_server(
  tools_script = NULL,
  session_tools = FALSE,
  rscript = file.path(R.home("bin"), if (.Platform$OS.type == "windows") "Rscript.exe"
    else "Rscript")
)
}
\arguments{
\item{tools_script}{Character(1) or NULL.  Path to an \code{.R} script that,
when sourced, yields a \code{list()} of \code{ellmer::tool()} objects.  When
\code{NULL} only the built-in session tools are exposed (controlled by
\code{session_tools}).}

\item{session_tools}{Logical. Whether to expose the built-in \code{mcptools}
session-management tools (\code{list_r_sessions}, \code{select_r_session}).
Default \code{FALSE} for embedding use-cases.}

\item{rscript}{Character(1). Absolute path to the \code{Rscript} binary.
Defaults to the binary of the \emph{currently running} R installation
(\code{Rscript} on Linux/macOS, \code{Rscript.exe} on Windows), resolved via
\code{R.home("bin")} — always an absolute path, independent of \code{PATH}.
Falls back to \code{Sys.which("Rscript")} if that path does not exist.
Pass an explicit path to use a specific R installation.}
}
\value{
A named list with \code{type}, \code{command}, and \code{args} suitable for use
as a value inside \code{ClaudeAgentOptions(mcp_servers = list(...))}.
}
\description{
Builds a \code{mcp_servers} list entry that launches an R subprocess running
\code{mcptools::mcp_server()} over stdio.  Requires the \code{mcptools} and \code{ellmer}
packages to be installed on the system.
}
\details{
\subsection{Defining tools}{

Create a standalone \code{.R} script that returns a \code{list()} of \code{ellmer::tool()}
objects (this is the value \code{mcptools::mcp_server()} accepts via its
\code{tools} argument):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# my_tools.R
library(ellmer)
list(
  ellmer::tool(
    fun         = function(a, b) a + b,
    description = "Add two numbers",
    arguments   = list(
      a = ellmer::type_number("First number"),
      b = ellmer::type_number("Second number")
    )
  )
)
}\if{html}{\out{</div>}}

Then pass the script path:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options <- ClaudeAgentOptions(
  mcp_servers   = list(my_tools = r_mcp_server("my_tools.R")),
  allowed_tools = "mcp__my_tools__add"
)
}\if{html}{\out{</div>}}
}
}
\examples{
# Create an MCP server entry from a tools script
entry <- r_mcp_server("my_tools.R")
entry$type    # "stdio"
entry$command # path to Rscript

# Use in ClaudeAgentOptions
\dontrun{
opts <- ClaudeAgentOptions(
  mcp_servers   = list(my_tools = r_mcp_server("my_tools.R")),
  allowed_tools = "mcp__my_tools__add"
)
}
}
