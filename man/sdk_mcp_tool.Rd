% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdk_mcp.R
\name{sdk_mcp_tool}
\alias{sdk_mcp_tool}
\title{Define an in-process SDK MCP tool}
\usage{
sdk_mcp_tool(name, description, input_schema, handler, annotations = NULL)
}
\arguments{
\item{name}{Character(1). Unique tool identifier Claude uses to call it.}

\item{description}{Character(1). What the tool does (helps Claude decide).}

\item{input_schema}{List. Either a full JSON Schema (a list with \code{type} and
\code{properties}), or a shorthand mapping parameter names to a type string
(e.g. \code{list(x = "number", label = "string")}) or to a partial schema
(e.g. \code{list(x = list(type = "number", description = "..."))}).}

\item{handler}{Function of one argument (a named list of the call
arguments). It should return a list with a \code{content} field —
\code{list(content = list(list(type = "text", text = "...")))} — and may set
\code{isError = TRUE}. A bare character string is also accepted and wrapped as
text content.}

\item{annotations}{Optional list of MCP tool annotations.}
}
\value{
An object of class \code{SdkMcpTool}.
}
\description{
Creates a tool that runs \emph{inside} the R SDK process. Pass the result to
\code{\link[=create_sdk_mcp_server]{create_sdk_mcp_server()}} and register the server via
\code{ClaudeAgentOptions(mcp_servers = ...)}. This is the R analogue of the
Python SDK's \code{tool()} decorator; it is named \code{sdk_mcp_tool()} (rather than
\code{tool()}) to avoid masking \code{ellmer::tool()}.
}
\examples{
greet <- sdk_mcp_tool(
  name = "greet", description = "Greet a user",
  input_schema = list(name = "string"),
  handler = function(args) {
    list(content = list(list(type = "text",
                             text = paste0("Hello, ", args$name, "!"))))
  }
)
}
\seealso{
\code{\link[=create_sdk_mcp_server]{create_sdk_mcp_server()}}
}
