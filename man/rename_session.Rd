% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_mutations.R
\name{rename_session}
\alias{rename_session}
\title{Rename a session}
\usage{
rename_session(session_id, title, directory = NULL)
}
\arguments{
\item{session_id}{Character. UUID of the session.}

\item{title}{Character. New title. Leading/trailing whitespace is stripped;
must be non-empty after stripping.}

\item{directory}{Character or NULL. Project directory (same semantics as
\code{list_sessions(directory = ...)}). When \code{NULL}, all project directories
are searched.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Appends a \code{custom-title} JSONL entry to the session file.
\code{list_sessions()} reads the LAST custom-title from the tail, so repeated
calls are safe — most recent wins.
}
\examples{
\donttest{
sessions <- list_sessions(limit = 1L)
if (length(sessions) > 0) {
  rename_session(sessions[[1]]$session_id, "My renamed session")
}
}
}
