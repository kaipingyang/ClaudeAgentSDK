% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sessions.R
\name{list_sessions}
\alias{list_sessions}
\title{List Claude Code sessions}
\usage{
list_sessions(
  directory = NULL,
  limit = NULL,
  offset = 0L,
  include_worktrees = TRUE
)
}
\arguments{
\item{directory}{Character or NULL. Project directory path.  When provided,
only sessions for that project (and its git worktrees when
\code{include_worktrees = TRUE}) are returned.  When \code{NULL}, all sessions
across all projects are returned.}

\item{limit}{Integer or NULL. Maximum number of sessions to return.}

\item{offset}{Integer. Number of sessions to skip (for pagination).}

\item{include_worktrees}{Logical. Scan git worktrees (default \code{TRUE}).}
}
\value{
List of \code{SDKSessionInfo} objects sorted by \code{last_modified}
descending.
}
\description{
Scans \verb{~/.claude/projects/} (or the project-specific sub-directory) for
\code{.jsonl} session files and extracts metadata from \code{stat} + head/tail reads
— no full JSONL parsing required.
}
\examples{
\donttest{
# All sessions
sessions <- list_sessions(limit = 5L)
length(sessions)

# Sessions for a specific project
sessions <- list_sessions(directory = getwd(), limit = 10L)
if (length(sessions) > 0) cat(sessions[[1]]$session_id, "\n")
}
}
