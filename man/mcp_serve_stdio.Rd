% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdk_mcp.R
\name{mcp_serve_stdio}
\alias{mcp_serve_stdio}
\title{Serve SDK MCP tools over stdio (newline-delimited JSON-RPC)}
\usage{
mcp_serve_stdio(server, input = NULL, output = stdout())
}
\arguments{
\item{server}{An \code{McpSdkServerConfig} from \code{\link[=create_sdk_mcp_server]{create_sdk_mcp_server()}}.}

\item{input, output}{Connections to read requests from / write responses to.
Default stdin/stdout. Exposed for testing.}
}
\value{
Invisibly \code{NULL} when the input stream reaches EOF.
}
\description{
Runs a blocking read/serve loop that speaks the MCP stdio transport
(newline-delimited JSON-RPC 2.0) for a server created with
\code{\link[=create_sdk_mcp_server]{create_sdk_mcp_server()}}. Intended to be the body of a small \code{Rscript}
launched by the CLI as an \strong{external stdio} MCP server (register it with
\code{type = "stdio"} in \code{mcp_servers}).
}
\details{
Unlike an in-process (\code{type = "sdk"}) server, a stdio server is initialized
by the CLI at startup and does \strong{not} exhibit the ~50s first-message stall
that in-process servers hit when the client connection idles before the first
message. It depends only on \code{jsonlite} — no \code{ellmer}/\code{curl}.
}
