% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{ClaudeAgentOptions}
\alias{ClaudeAgentOptions}
\title{Create ClaudeAgentOptions}
\usage{
ClaudeAgentOptions(
  tools = NULL,
  allowed_tools = character(),
  system_prompt = NULL,
  mcp_servers = list(),
  permission_mode = NULL,
  continue_conversation = FALSE,
  resume = NULL,
  session_id = NULL,
  max_turns = NULL,
  max_budget_usd = NULL,
  disallowed_tools = character(),
  model = NULL,
  fallback_model = NULL,
  betas = character(),
  permission_prompt_tool_name = NULL,
  cwd = NULL,
  cli_path = NULL,
  settings = NULL,
  add_dirs = list(),
  env = list(),
  extra_args = list(),
  max_buffer_size = NULL,
  stderr = NULL,
  can_use_tool = NULL,
  hooks = NULL,
  user = NULL,
  include_partial_messages = FALSE,
  fork_session = FALSE,
  agents = NULL,
  setting_sources = NULL,
  sandbox = NULL,
  plugins = list(),
  max_thinking_tokens = NULL,
  thinking = NULL,
  effort = NULL,
  output_format = NULL,
  enable_file_checkpointing = FALSE,
  task_budget = NULL
)
}
\arguments{
\item{tools}{Character vector, named list (\code{type="preset"}), or NULL.
Base set of tools available to Claude.  Passing \code{list(type="preset", preset="claude_code")} maps to \verb{--tools default}.}

\item{allowed_tools}{Character vector. Additional tools to allow beyond
the base set (\code{--allowedTools}).}

\item{system_prompt}{Character, named list (\code{type="preset"} /
\code{type="file"}), or NULL.  System prompt text or configuration.}

\item{mcp_servers}{Named list of MCP server configs, or character path to
an MCP config file.}

\item{permission_mode}{Character or NULL.  One of \code{"default"},
\code{"acceptEdits"}, \code{"bypassPermissions"}, \code{"plan"}, \code{"dontAsk"}, \code{"auto"}.}

\item{continue_conversation}{Logical. Continue the most recent session
(\code{--continue}).}

\item{resume}{Character or NULL. Resume a specific session ID
(\code{--resume}).}

\item{session_id}{Character or NULL. Explicit session ID to use
(\code{--session-id}).}

\item{max_turns}{Integer or NULL. Maximum conversation turns
(\code{--max-turns}).}

\item{max_budget_usd}{Numeric or NULL. Budget cap in USD
(\code{--max-budget-usd}).}

\item{disallowed_tools}{Character vector. Tools to block
(\code{--disallowedTools}).}

\item{model}{Character or NULL. Model ID (\code{--model}).}

\item{fallback_model}{Character or NULL. Fallback model ID
(\code{--fallback-model}).}

\item{betas}{Character vector. SDK beta feature flags (\code{--betas}).}

\item{permission_prompt_tool_name}{Character or NULL. Tool name used for
the permission prompt control protocol (\code{--permission-prompt-tool}).}

\item{cwd}{Character or NULL. Working directory for the Claude process.}

\item{cli_path}{Character or NULL. Explicit path to the \code{claude} binary.}

\item{settings}{Character or NULL. Path to a settings JSON file, or a
raw JSON string (\code{--settings}).}

\item{add_dirs}{List of character paths. Additional directories to add
(\code{--add-dir}).}

\item{env}{Named list of character strings. Extra environment variables
for the subprocess.}

\item{extra_args}{Named list. Arbitrary extra CLI flags.  Each name
becomes \verb{--<name>}; the value is the flag value (\code{NULL} for boolean
flags).}

\item{max_buffer_size}{Integer or NULL. Maximum bytes to buffer from CLI
stdout before raising an error (default 1 MB).}

\item{stderr}{Function(line) or NULL. Callback receiving each stderr line.}

\item{can_use_tool}{Function or NULL. Permission callback with signature
\verb{function(tool_name, tool_input, context)} returning a
\code{PermissionResultAllow} or \code{PermissionResultDeny}.}

\item{hooks}{Named list of HookMatcher lists keyed by hook event name.}

\item{user}{Character or NULL. OS user to run the subprocess as.}

\item{include_partial_messages}{Logical. Emit partial streaming messages
(\code{--include-partial-messages}).}

\item{fork_session}{Logical. Fork resumed session to a new session ID
(\code{--fork-session}).}

\item{agents}{Named list of agent definitions.}

\item{setting_sources}{Character vector or NULL. Setting sources to load
(\code{--setting-sources}).}

\item{sandbox}{List or NULL. Sandbox settings dict.}

\item{plugins}{List of plugin configs (\code{type="local"}, \code{path=...}).}

\item{max_thinking_tokens}{Integer or NULL. \strong{Deprecated}; use \code{thinking}.}

\item{thinking}{Named list or NULL. Thinking config
(\code{list(type="adaptive")}, \code{list(type="enabled", budget_tokens=N)},
\code{list(type="disabled")}).}

\item{effort}{Character or NULL. Thinking depth: \code{"low"}, \code{"medium"},
\code{"high"}, \code{"max"}.}

\item{output_format}{Named list or NULL. Structured output format, e.g.
\code{list(type="json_schema", schema=list(...))}.}

\item{enable_file_checkpointing}{Logical. Track file changes for rewind
support.}

\item{task_budget}{Named list or NULL. API-side task budget, e.g.
\code{list(total = 10000L)}.}
}
\value{
Object of class \code{ClaudeAgentOptions}.
}
\description{
Constructs an options object controlling every aspect of a Claude Code
session.  Mirrors the Python SDK's \code{ClaudeAgentOptions} dataclass
field-for-field.
}
