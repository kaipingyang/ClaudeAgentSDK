% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types.R
\name{ServerToolUseBlock}
\alias{ServerToolUseBlock}
\title{Create a ServerToolUseBlock}
\usage{
ServerToolUseBlock(id, name, input)
}
\arguments{
\item{id}{Character. Tool use ID.}

\item{name}{Character. Server tool name (one of \code{SERVER_TOOL_NAMES}).}

\item{input}{List. Tool input parameters.}
}
\value{
Object of class \code{ServerToolUseBlock}.
}
\description{
Server-side tool use block (e.g. advisor, web_search, web_fetch). These are
tools the API executes server-side on the model's behalf, so they appear in
the message stream alongside regular \code{tool_use} blocks but the caller never
needs to return a result. \code{name} is a discriminator — branch on it to know
which server tool was invoked.
}
\examples{
blk <- ServerToolUseBlock("srv1", "web_search", list(query = "R language"))
blk$name
}
