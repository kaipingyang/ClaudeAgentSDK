% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{claude_query}
\alias{claude_query}
\title{Query Claude Code (streaming generator)}
\usage{
claude_query(prompt, options = ClaudeAgentOptions(), transport = NULL)
}
\arguments{
\item{prompt}{Character(1) or list. Prompt text, or a list of content
blocks.}

\item{options}{A \code{ClaudeAgentOptions} from \code{\link[=ClaudeAgentOptions]{ClaudeAgentOptions()}}.}

\item{transport}{Optional \code{SubprocessCLITransport} R6 object. When
supplied, \code{connect()} is NOT called automatically — the caller must
have already connected.}
}
\value{
A \code{coro} generator yielding message objects (see types.R).
}
\description{
Creates a \code{SubprocessCLITransport}, connects to the CLI, sends the
prompt, and returns a \code{coro} generator that yields typed message objects.
The generator terminates automatically after the \code{ResultMessage}.
}
\details{
The caller is responsible for disconnecting the transport after the
generator is exhausted.  For a simpler synchronous API see \code{\link[=claude_run]{claude_run()}}.
}
