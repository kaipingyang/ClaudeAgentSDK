# Hook-specific output for PermissionRequest hook

Hook-specific output for PermissionRequest hook

## Usage

``` r
PermissionRequestHookSpecificOutput(decision)
```

## Arguments

- decision:

  List. Required. Permission decision payload.

## Value

Object of class `PermissionRequestHookSpecificOutput`.
