# Valid server-side tool names (`ServerToolName`)

The set of tools the API executes server-side on the model's behalf.
Branch on `ServerToolUseBlock$name` to know which server tool was
invoked.

## Usage

``` r
SERVER_TOOL_NAMES
```
