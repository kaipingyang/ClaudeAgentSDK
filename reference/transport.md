# SubprocessCLITransport

R6 class that manages the Claude Code CLI subprocess and implements the
full bidirectional control protocol. Mirrors
`_internal/transport/subprocess_cli.py` plus the `Query` class from
`_internal/query.py`.
