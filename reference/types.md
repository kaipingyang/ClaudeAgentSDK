# Message Type Constructors

Lightweight S3 constructors mirroring every dataclass defined in the
Python SDK's `types.py`. All objects are named lists with a `class`
attribute. Fields match Python field names exactly (snake_case).
