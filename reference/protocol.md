# Protocol — JSON Parsing and Message Building

Converts raw newline-delimited JSON lines from the Claude Code CLI into
typed R objects, and builds outgoing JSON messages. Mirrors
`_internal/message_parser.py`.
